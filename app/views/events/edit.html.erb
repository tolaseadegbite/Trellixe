<% content_for :title, "Edit Event" %>

<%= render "shared/modal" do %>
  <div data-action="turbo:submit-end->modal#submitEnd">
    <h2 class="font-display text-lg font-semibold">Edit event</h2>
    <div class="mt-4">
      <%= render "form", event: @event %>
    </div>
  </div>
<% end %>
