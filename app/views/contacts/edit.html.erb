<% content_for :title, "Edit Contact" %>

<%= render "shared/modal" do %>
  <div data-action="turbo:submit-end->modal#submitEnd">
    <h2 class="mb-4 font-display text-xl font-bold tracking-tight">Edit contact</h2>
    <%= render "form", contact: @contact %>
  </div>
<% end %>
