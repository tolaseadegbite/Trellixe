<%# Replace the unified row using its unique ID %>
<%= turbo_stream.replace dom_id(@contact), partial: "contacts/contact_row", locals: { contact: @contact } %>

<%= turbo_stream.update "flash_messages", partial: "shared/flash", locals: { notice: flash.now[:notice], alert: flash.now[:alert] } %>
