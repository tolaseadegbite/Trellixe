<%# 1. Update Flash %>
<%= turbo_stream.update "flash_messages", partial: "shared/flash" %>

<%# 2. Re-render the row %>
<%= turbo_stream.replace dom_id(@membership), partial: "members/membership", locals: { membership: @membership } %>